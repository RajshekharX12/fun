BOT_TOKEN=123456:ABC-your-telegram-bot-token
OWNER_ID=000000000              # your Telegram numeric user ID (not username)
TZ=Asia/Kolkata                 # server timezone label (used in logs & timestamps)

# Optional features (leave empty to disable gracefully)
TRANSLATE_ENABLED=false         # true/false
TRANSLATE_TO=en                 # e.g. en, hi, ru
RATE_LIMIT_PER_MIN=20           # per user
WHITELIST_MODE=false            # if true, only whitelisted users can DM you
AWAY_TEXT=                       # optional: auto-reply text (sent at most once/hour per user)
